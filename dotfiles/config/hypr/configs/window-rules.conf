# TAGS - add apps under appropriate tag to use the same settings
# browser tags
windowrule = match:class ^([Ff]irefox|org.mozilla.firefox|[Ff]irefox-esr|[Ff]irefox-bin)$, tag +browser
windowrule = match:class ^([Gg]oogle-chrome(-beta|-dev|-unstable)?)$, tag +browser
windowrule = match:class ^(chrome-.+-Default)$, tag +browser
windowrule = match:class ^([Cc]hromium)$, tag +browser
windowrule = match:class ^([Mm]icrosoft-edge(-stable|-beta|-dev|-unstable))$, tag +browser
windowrule = match:class ^(Brave-browser(-beta|-dev|-unstable)?)$, tag +browser
windowrule = match:class ^([Tt]horium-browser|[Cc]achy-browser)$, tag +browser
windowrule = match:class ^(zen-alpha|zen)$, tag +browser

# terminal tags
windowrule = match:class ^(Alacritty|kitty|com.mitchellh.ghostty|org.wezfurlong.wezterm|.*-dropterm)$, tag +terminal

# file_manager tags
windowrule = match:class ^([Tt]hunar|org.gnome.Nautilus|[Pp]cmanfm|[Pp]cmanfm-qt)$, tag +file_manager

# code_editor tags
windowrule = match:class ^(codium|codium-url-handler|VSCodium)$, tag +code_editor
windowrule = match:class ^(VSCode|code|code-url-handler)$, tag +code_editor
windowrule = match:class ^(jetbrains-.+)$, tag +code_editor
windowrule = match:class ^(dev.zed.Zed|antigravity)$, tag +code_editor
windowrule = match:class ^(neovide)$, tag +code_editor

# email tags
windowrule = match:class ^([Tt]hunderbird|org.mozilla.Thunderbird)$, tag +email
windowrule = match:class ^(eu.betterbird.Betterbird)$, tag +email
windowrule = match:class ^(org.gnome.Evolution)$, tag +email

# messenger tags
windowrule = match:class ^([Dd]iscord|[Ww]ebCord|[Vv]esktop)$, tag +messenger
windowrule = match:class ^([Ff]erdium)$, tag +messenger
windowrule = match:class ^([Ww]hatsapp-for-linux)$, tag +messenger
windowrule = match:class ^(org.telegram.desktop|io.github.tdesktop_x64.TDesktop)$, tag +messenger
windowrule = match:class ^(teams-for-linux)$, tag +messenger
windowrule = match:class ^(im.riot.Riot|Element)$, tag +messenger

# media_player tags
windowrule = match:class ^([Mm]pv|vlc)$, tag +media_player

# music_player tags
windowrule = match:class ^(org.gnome.Lollypop|io.bassi.Amberol|com.github.neithern.g4music|[Aa]udacious)$, tag +music_player
windowrule = match:class ^([Ss]potify)$, tag +music_player

# image_editor tags
windowrule = match:class ^(gimp|com.github.PintaProject.Pinta)$, tag +image_editor

# video_editor tags
windowrule = match:class ^(org.pitivi.Pitivi|org.kde.kdenlive|org.shotcut.Shotcut)$, tag +video_editor

# image_viewer tags
windowrule = match:class ^(imv|org.gnome.gThumb|org.gnome.Shotwell)$, tag +image_viewer

# document_viewer tags
windowrule = match:class ^(org.gnome.Evince|org.pwmt.zathura|org.kde.Okular)$, tag +document_viewer

# system_monitor
windowrule = match:class ^(gnome-system-monitor|org.gnome.SystemMonitor)$, tag +system_monitor
windowrule = match:class ^(io.missioncenter.MissionCenter|net.nokyan.Resources)$, tag +system_monitor

# screenshare tags
windowrule = match:class ^(com.obsproject.Studio)$, tag +screenshare

# wallpaper_manager tags
windowrule = match:class ^(org.Waytrogen.Waytrogen|waypaper)$, tag +wallpaper_manager

# game tags
windowrule = match:class ^(org.gnome.Chess)$, tag +games
windowrule = match:class ^(gamescope)$, tag +games
windowrule = match:class ^(steam_app_\\d+)$, tag +games

# gamestore tags
windowrule = match:class ^([Ss]team)$, tag +gamestore
windowrule = match:title ^([Ll]utris)$, tag +gamestore
windowrule = match:class ^(com.heroicgameslauncher.hgl)$, tag +gamestore

# settings tags
windowrule = match:title ^(ROG Control)$, tag +settings
windowrule = match:class ^(wihotspot(-gui)?)$, tag +settings
windowrule = match:class ^([Bb]aobab|org.gnome.[Bb]aobab)$, tag +settings
windowrule = match:class ^(gnome-disks|wihotspot(-gui)?)$, tag +settings
windowrule = match:title (Kvantum Manager), tag +settings
windowrule = match:class ^(file-roller|org.gnome.FileRoller)$, tag +settings
windowrule = match:class ^(nm-applet|nm-connection-editor|blueman-manager)$, tag +settings
windowrule = match:class ^(pavucontrol|org.pulseaudio.pavucontrol|com.saivert.pwvucontrol)$, tag +settings
windowrule = match:class ^(nwg-look|qt5ct|qt6ct)$, tag +settings
windowrule = match:class (xdg-desktop-portal-gtk), tag +settings
windowrule = match:class ^(org.kde.polkit-kde-authentication-agent-1)$, tag +settings
windowrule = match:class ^([Rr]ofi)$, tag +settings
windowrule = match:class ^(btrfs-assistant)$, tag +settings
windowrule = match:class ^(timeshift-gtk)$, tag +settings

# multimedia tags
windowrule = match:class ^([Aa]udacious)$, tag +multimedia

# multimedia-video tags
windowrule = match:class ^([Mm]pv|vlc)$, tag +multimedia_video
windowrule = match:class ^(eog|org.gnome.Loupe)$, tag +viewer

# start apps on specific workspaces
windowrule = match:tag code_editor*, workspace 1
windowrule = match:tag browser*, workspace 2
windowrule = match:tag file_manager*, workspace 3
windowrule = match:tag messenger*, workspace 6
windowrule = match:tag music_player*, workspace 7
windowrule = match:tag screenshare*, workspace 4 silent
windowrule = match:tag gamestore*, workspace 5
windowrule = match:class ^(virt-manager)$, workspace 6 silent
windowrule = match:class ^(.virt-manager-wrapped)$, workspace 6 silent
windowrule = match:tag im*, workspace 7
windowrule = match:class obsidian, workspace 8
windowrule = match:tag games*, workspace 8
windowrule = match:tag multimedia*, workspace 9 silent

# Some special override rules
windowrule = match:tag multimedia_video, no_blur on
windowrule = match:tag multimedia_video, opacity 1.0
windowrule = match:tag multimedia, no_blur on
windowrule = match:tag multimedia, opacity 1.0

# POSITION
windowrule = match:tag KooL_Cheat, center on
windowrule = match:tag KooL-Settings, center on
windowrule = match:title ^(ROG Control)$, center on
windowrule = match:title ^(Keybindings)$, center on
windowrule = match:class ^(pavucontrol|org.pulseaudio.pavucontrol|com.saivert.pwvucontrol)$, center on
windowrule = match:class ^([Ff]erdium)$, center on

# windowrule to avoid idle for fullscreen apps
windowrule = match:fullscreen true, idle_inhibit fullscreen
windowrule = idle_inhibit fullscreen, match:fullscreen 1
windowrule = idle_inhibit fullscreen, match:class ^(*)$
windowrule = idle_inhibit fullscreen, match:title ^(*)$

# FLOAT
windowrule = match:tag KooL_Cheat, float on
windowrule = match:tag wallpaper, float on, center on
windowrule = match:tag settings, float on, center on
windowrule = match:tag viewer, float on, center on
windowrule = match:tag KooL-Settings, float on, center on
windowrule = match:class ([Zz]oom|onedriver|onedriver-launcher), float on
windowrule = match:class (org.gnome.Calculator|qalculate-gtk), float on
# windowrule = match:class ^(mpv|com.github.rafostar.Clapper)$, float on
windowrule = match:class ^([Qq]alculate-gtk)$, float on
windowrule = match:class ^([Ff]erdium)$, float on

# popups and dialogue
windowrule = match:title ^(Authentication Required)$, float on, center on
windowrule = match:class ^(com.heroicgameslauncher.hgl)$ match:title negative:(Heroic Games Launcher), float on
windowrule = match:class ^([Ss]team)$ match:title negative:^([Ss]team)$, float on
windowrule = match:title ^(Add Folder to Workspace)$, float on, size (monitor_w*0.7) (monitor_h*0.6), center on
windowrule = match:title ^(Save As)$, float on, size (monitor_w*0.7) (monitor_h*0.6), center on
windowrule = match:initial_title (Open Files), float on, size (monitor_w*0.7) (monitor_h*0.6)
windowrule = match:title ^(SDDM Background)$, float on, center on, size (monitor_w*0.16) (monitor_h*0.12)
windowrule = match:class ^(yad)$, float on, center on, size (monitor_w*0.2) (monitor_h*0.2)
windowrule = match:class ^(hyprland-donate-screen)$, float on, center on

# SIZE
windowrule = match:tag wallpaper, size (monitor_w*0.7) (monitor_h*0.7)
windowrule = match:tag settings, size (monitor_w*0.7) (monitor_h*0.7)
windowrule = match:class ^([Ff]erdium)$, size (monitor_w*0.6) (monitor_h*0.7)

# BLUR & FULLSCREEN
windowrule = match:tag games, no_blur on, fullscreen 0
windowrule = match:tag games, fullscreen 0

# This not gonna take the focus to the window that appears when
# hovering over some of the parts of the IntelliJ Products
windowrule = match:class ^(jetbrains-*), no_initial_focus on
windowrule = match:title ^(wind.*)$, no_initial_focus on

# LAYER RULES
layerrule = match:namespace rofi, blur on
layerrule = match:namespace notifications, blur on
layerrule = match:namespace quickshell:overview, blur on
layerrule = match:namespace quickshell:overview, ignore_alpha 0.5

# Named rules for special cases
windowrule {
    name = Whatsapp-zapzap
    match:class = ^([Ww]hatsapp-for-linux|ZapZap|com.rtosta.zapzap)$
    size = (monitor_w*0.6) (monitor_h*0.7)
    center = on
}
windowrule {
    name = Picture-in-Picture
    match:title = ^(Picture-in-Picture)$
    float = on
    move = 72% 7%
    opacity = 0.95 0.75
    pin = on
    keep_aspect_ratio = on
    size = (monitor_w*0.3) (monitor_h*0.3)
}

# Thunar copy progress dialog
windowrule {
    name = Thunar-Progress-bar
    match:class = ^(thunar)$
    match:title = ^(File Operation Progress)$
    float = on
    center = on
    size = (monitor_w*0.26) (monitor_h*0.18)
}

# Ignore maximize requests from all apps. You'll probably like this.
windowrule {
    name = suppress_maximize_events
    match:class = .*

    suppress_event = maximize
}

windowrule {
    # Fix some dragging issues with XWayland
    name = fix_xwayland_drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = true
}
layerrule = no_anim on, match:namespace selection

# Noctalia shell specific
layerrule {
  name = noctalia_background_blur
  match:namespace = noctalia-background-.*$
  ignore_alpha = 0.5
  blur = true
  blur_popups = true
}
layerrule {
  name = noctalia_region_selector_no_anim
  match:namespace = noctalia-shell:regionSelector
  no_anim = on
}




windowrule = match:class ^(zen|brave-browser|[Ff]irefox|org.mozilla.firefox|[Ff]irefox-esr|[Ff]irefox-bin|google-chrome)$, tag +browser
windowrule = match:class ^(code|neovide)$, tag +code_editor
windowrule = match:class ^(thunar|org.gnome.Nautilus)$, tag +file_manager
windowrule = match:class ^(org.telegram.desktop|discord)$, tag +messaging
windowrule = match:class ^([Ss]potify)$, tag +music
windowrule = match:class ^(wlogout)$, tag +logout_screen
windowrule = match:class ^(nwg-look|qt5ct)$, tag +settings
windowrule = match:class ^(org.pulseaudio.pavucontrol)$, tag +settings
windowrule = match:class ^(blueman-manager)$, tag +settings
windowrule = match:class ^(nm-connection-editor)$, tag +settings
windowrule = match:class ^(file-roller|file_progress)$, tag +settings
windowrule = match:class ^(confirm|dialog|download|notification|error|splash|confirmreset)$, tag +settings
windowrule = match:class ^(com.ml4w.hyprlandsettings)$, tag +settings
windowrule = match:class ^(qalculate-gtk)$, tag +settings
windowrule = match:class ^(termfilechooser)$, tag +settings
windowrule = match:class ^(bluetui|impalatui)$, tag +settings
windowrule = match:title ^(Open File|Save File|branchdialog|.*wants to save.*|.*wants to open.*)$, tag +settings
windowrule = match:title ^(Add Folder to Workspace|Save As)$, tag +settings
windowrule = match:title ^(Authentication Required)$, tag +settings
windowrule = match:initial_title ^(Open Files)$, tag +settings
windowrule = match:tag code_editor*, workspace 1
windowrule = match:tag browser*, workspace 2
windowrule = match:tag file_manager*, workspace 3
windowrule = match:tag messaging*, workspace 6
windowrule = match:tag music*, workspace 7
windowrule = match:tag settings*, float on
windowrule = match:tag settings*, center on
# windowrule = match:tag settings*, size 50% # This is not working as same as below.
windowrule = match:tag settings*, size 960 540
windowrule = match:tag logout_screen*, fullscreen on
windowrule = match:title ^(Picture-in-Picture)$, pin on
windowrule = match:title ^(Picture-in-Picture)$, keep_aspect_ratio on
windowrule = match:class ^(kitty|com.mitchellh.ghostty|org.wezfurlong.wezterm|neovide|org.pwmt.zathura), opacity 0.8 0.8
# windowrule = opacity 0.8 0.7, tag:browser*
windowrule = match:tag messaging*, opacity 0.8 0.8
windowrule = match:tag music*, opacity 0.8 0.8
windowrule = match:fullscreen 1, idle_inhibit fullscreen
windowrule = match:class ^(kitty|com.mitchellh.ghostty|org.wezfurlong.wezterm)$, persistent_size on
# windowrule = match:class .* suppressevent, maximize on

windowrule {
    # Ignore maximize requests from all apps. You'll probably like this.
    name = suppress_maximize_events
    match:class = .*

    suppress_event = maximize
}
windowrule {
    # Fix some dragging issues with XWayland
    name = fix_xwayland_drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = true
}
layerrule = no_anim on, match:namespace selection

# layerrule = noanim, fuzzel

# windowrule = float, title:^(Add Folder to Workspace)$
# windowrule = size 70% 60%, title:^(Add Folder to Workspace)$
# windowrule = center, title:^(Add Folder to Workspace)$
# windowrule = float, title:^(Save As)$
# windowrule = size 60%, title:^(Save As)$
# windowrule = center, title:^(Save As)$
# windowrule = float, initialTitle:(Open Files)
# windowrule = size 70% 60%, initialTitle:(Open Files)

# windowrule = float on, match:class ^(kitty)
# windowrule = move 25% 25%, match:class ^(kitty)
# windowrule = size 50%, match:class ^(kitty)
# windowrule = center on, match:class ^(kitty)

layerrule {
  name = noctalia_background_blur
  match:namespace = noctalia-background-.*$
  ignore_alpha = 0.5
  blur = true
  blur_popups = true
}

layerrule {
  name = noctalia_region_selector_no_anim
  match:namespace = noctalia-shell:regionSelector
  no_anim = on
}

# blurls = waybar
# blurls = rofi
# layerrule = match:class waybar, blur on
# layerrule = match:class waybar, blur_popups on
# layerrule = match:class waybar, ignore_alpha 0.2
# layerrule = match:class rofi, blur on
# layerrule = match:class rofi, ignore_alpha 0.2
# layerrule = match:class rofi, no_anim on
# layerrule = match:class notifications, blur on
# layerrule = match:class notifications, ignore_alpha 0.2
# layerrule = match:class notifications, no_anim on
# layerrule = match:class logout_dialog, blur on
# layerrule = match:class logout_dialog, ignore_alpha 0.2
# layerrule = match:class logout_dialog, no_anim on
# layerrule = blur, waybar
# layerrule = blurpopups, waybar
# layerrule = ignorealpha 0.2, waybar
# layerrule = blur, rofi
# layerrule = ignorealpha 0.2, rofi
# layerrule = noanim, rofi
# layerrule = blur, notifications
# layerrule = ignorealpha 0.2, notifications
# layerrule = noanim, notifications
# layerrule = blur, logout_dialog
# layerrule = ignorealpha 0.2, logout_dialog
# layerrule = noanim, logout_dialog

# windowrule = scrolltouchpad 1.5, class:kitty

